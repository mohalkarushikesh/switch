# Base URL of the backend API.
# Leave EMPTY in local dev — Vite proxies /api to localhost:8000 for you.
# In production (S3), set this to your deployed backend's URL, e.g.
#   VITE_API_BASE=https://api.your-domain.com
VITE_API_BASE=
