# Core dependencies for the Custodian MVP
litellm>=1.40.0        # unified gateway to LLM providers (OpenAI, Groq, ...)
pydantic>=2.6.0        # typed data models & validation
python-dotenv>=1.0.0   # load configuration from a .env file

# API service
fastapi>=0.110.0       # REST API framework
uvicorn>=0.29.0        # ASGI server to run the API

# Optional: NLP-based PII redaction (CUSTODIAN_PII_BACKEND=presidio).
# Also requires a spaCy model:  python -m spacy download en_core_web_sm
# presidio-analyzer>=2.2.0

# Optional: model tracking (CUSTODIAN_MLFLOW_URI). The skinny client is enough
# for logging to a file store or tracking server.
# mlflow-skinny>=3.0.0

# Dev / testing
pytest>=8.0.0
httpx>=0.27.0          # required by FastAPI's TestClient
